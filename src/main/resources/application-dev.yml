server:
  port: 8080

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mybatis?useUnicode=true&amp;characterEncoding=UTF8&amp;nullCatalogMeansCurrent=true
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:org/lanqiao/cart/mapping/*.xml
  type-aliases-package: org.lanqiao.cart.entity

#showSql
logging:
  level:
    org:
    lanqiao:
    cart:
        mapper : debug